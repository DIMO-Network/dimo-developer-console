---
context: default
phase: frontend-redesign
task: 9
total_tasks: 10
status: in_progress
last_updated: 2026-05-28T21:04:48Z
branch: feat/frontend-redesign
plan: docs/superpowers/plans/2026-05-27-frontend-redesign.md
---

# Frontend Redesign — Paused at Task 9/10

No blocking constraints. Clean state, 8 commits merged, 2 tasks remain.

<current_state>
Branch `feat/frontend-redesign`. 8 of 10 tasks committed. Paused due to disk full (ENOSPC) + context exhaustion. Disk is now free (~4GB). Task 9 (Sonner) not started. Task 10 (AppCard cleanup) not started. Nothing uncommitted.
</current_state>

<completed_work>

- Task 1 `d7249a1` — shadcn/ui init + DIMO Teal CSS tokens (globals.css, tailwind.config.ts, src/lib/utils.ts, components.json)
- Task 2 `96501bf` — ThemeProvider in RootLayout + ThemeToggle in Header (dark/light mode, keyboard accessible)
- Task 3 `b03af36` — isSidebarCollapsed + setSidebarCollapsed in LayoutContext + withLayout HOC with localStorage persistence
- Task 4 `9256a33` — Sidebar redesign: NavSection groups (Workspace/Resources), collapse button, MenuItem with token classes, AuthorizedLayout width transition
- Task 5 `0e36689` — Header.css token migration, CreditsWidget small variant → teal pill showing USD balance
- Task 6 `fb7fdf1` — Button wraps shadcn HTML button; Button.css uses semantic tokens; all variant selectors preserved
- Task 7 `23fa078` — TextField→Input, TextArea→Textarea, Toggle→Switch, CheckboxField→Checkbox; SelectField skipped (react-hook-form incompatibility)
- Task 8 `59c41a5` — Modal replaced with shadcn Dialog; shadcn's auto-generated close button removed to prevent double X

</completed_work>

<remaining_work>

## Task 9: Notification System → Sonner

**Notification API:** `setNotification(message: string, title: string, type: 'success'|'error'|'info')` — 3 positional args (NOT object form).

**Files to update:** ~44 files. Run this to find them:

```bash
grep -rl "setNotification\|useNotification" src/ --include="*.tsx" --include="*.ts" | grep -v "withNotifications.tsx\|notificationContext.ts\|useNotification.ts\|hooks/index.ts"
```

**Steps:**

1. `npx shadcn@latest add sonner --yes`
2. Add `<Toaster position="bottom-right" />` to `src/layouts/RootLayout/RootLayout.tsx` inside ThemeProvider
3. For each caller file: remove `useNotification` import + destructuring, add `import { toast } from 'sonner'`, replace `setNotification('msg', 'title', 'success')` → `toast.success('msg')` etc.
4. Remove `withNotifications` from `src/layouts/AuthorizedLayout/AuthorizedLayout.tsx` Providers chain
5. Remove `withNotifications` from `src/layouts/GuestLayout/GuestLayout.tsx` Providers chain
6. Commit: `"feat: replace withNotifications HOC and Toast with Sonner"`

**Mapping:**

- `type: 'success'` → `toast.success(message)`
- `type: 'error'` → `toast.error(message)`
- `type: 'info'` → `toast(message)`

## Task 10: AppCard Simplification + Token Cleanup

Per plan at `docs/superpowers/plans/2026-05-27-frontend-redesign.md` Task 10:

- Simplify AppCard (remove environment display)
- Update LicenseCard colors: `bg-surface-default`→`bg-card`, `bg-surface-raised`→`bg-accent`, `text-white`→`text-foreground`, etc.
- Scan for remaining `bg-black`, `bg-surface-*`, `text-white\b` and replace with token equivalents
- Full test suite + TypeScript check
- Commit: `"feat: simplify AppCard, replace hardcoded dark tokens with semantic classes"`

</remaining_work>

<decisions_made>

- **SelectField skipped**: Custom react-hook-form Controller dropdown not compatible with shadcn Select API — leave as-is
- **Table skipped**: Complex TanStack Table integration; `src/components/ui/table.tsx` available for future use
- **surface/cta lowercase `default`**: Existing code uses `bg-surface-default` (with suffix), NOT bare `bg-surface` — lowercase is intentional backward compat
- **src/components/ui/button.tsx rewritten**: shadcn init auto-generated `@base-ui/react/button` (wrong); replaced with standard `React.forwardRef` HTML button

</decisions_made>

<blockers>
None. Clean state.
</blockers>

## Required Reading (in order)

1. `docs/superpowers/plans/2026-05-27-frontend-redesign.md` — full plan with Task 9 and 10 step-by-step instructions
2. This file

## Infrastructure State

- Branch: `feat/frontend-redesign` (clean, nothing uncommitted)
- Disk: ~4GB free after `brew cleanup` + `npm cache clean`
- Tests: 122 passing, ~16 pre-existing failures (wagmi ESM + next-auth module issues — unrelated to this work)

<context>
Executing a 10-task frontend redesign plan using subagent-driven development. Each task: implementer subagent → spec review → quality review → fixes → next task. Tasks 1-8 all passed both reviews (some with minor fixes applied). The pattern works well. Task 9 is purely mechanical (search-replace across ~44 files) — the main complexity is finding all callers and handling the 3-arg API correctly.
</context>

<next_action>
Start with: `/gsd-resume-work` — it will read this file and orient you. Then execute Task 9 (Sonner migration) and Task 10 (token cleanup) using the subagent-driven-development skill to finish the plan.
</next_action>
