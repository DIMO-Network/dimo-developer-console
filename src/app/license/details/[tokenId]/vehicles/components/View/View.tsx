import { Section, SectionHeader } from '@/components/Section';

export const View = () => {
  return (
    <div>
      <Section>
        <SectionHeader title={'Vehicle Details'} />
      </Section>
    </div>
  );
};
