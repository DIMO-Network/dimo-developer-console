import { Suspense } from 'react';

import { View } from '@/app/sign-up/components/View';

const SignUp = () => {
  return (
    <Suspense>
      <View />
    </Suspense>
  );
};

export default SignUp;
