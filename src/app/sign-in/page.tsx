import { Suspense } from 'react';

import { View } from '@/app/sign-in/components/View';

const SignIn = () => {
  return (
    <Suspense>
      <View />
    </Suspense>
  );
};

export default SignIn;
